image: node:current-alpine
pages:
  cache:
    paths:
      - node_modules/
  script:
    - npm install -g @angular/cli@10.1.3
    - npm install
    - ng build --base-href="/$CI_PROJECT_PATH_SLUG/"
    - mv dist/locator/* public/
  artifacts:
    paths:
      - public
  only:
    - master
    - pages
